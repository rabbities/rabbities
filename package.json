{
  "scripts": {
    "deploy": "npm run build:web && npm run pages",
    "build:web": "flutter build web --web-renderer=canvaskit --release",
    "pages": "gh-pages -d build/web"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}